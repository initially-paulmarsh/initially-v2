export const CATEGORY_LABEL = { movie: 'Movie', proverb: 'Proverb', song: 'Song', book: 'Book' }

// Same emoji as the logo mark (Logo.jsx), so the tabs read as the same
// icon set rather than a different one.
export const CATEGORY_ICON = { movie: '🎬', proverb: '📜', song: '🎵', book: '📖' }
